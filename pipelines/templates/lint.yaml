parameters:
  - name: modulePath
    type: string
  - name: azureServiceConnection
    type: string

steps:
  - task: AzureCLI@2
    displayName: 'Lint ${{ parameters.modulePath }}'
    inputs:
      azureSubscription: ${{ parameters.azureServiceConnection }}
      scriptType: bash
      scriptLocation: scriptPath
      scriptPath: scripts/lint.sh
      arguments: '${{ parameters.modulePath }}'
